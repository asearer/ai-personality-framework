[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-personality-engine"
version = "0.1.0"
description = "A modular artificial personality and cognitive-agent engine"
readme = "PROMPT.md"
requires-python = ">=3.10"
dependencies = [
    # Add dependencies like pydantic or async libraries here later
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0"
]
